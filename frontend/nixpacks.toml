# Railway Nixpacks config — forces build within the frontend service only
providers = ["node"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
